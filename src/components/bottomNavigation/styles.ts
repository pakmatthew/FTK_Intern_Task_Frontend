import { style } from "typestyle";

export const FBottomNavigationDiv = style({
    alignItems: "center",
    display: "flex",
    flexDirection: "row",
    justifyContent: "space-between",
    width: "100%"
})